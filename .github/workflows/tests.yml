name: Test Website
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test HTML
        run: |
          curl -s https://cephasgm.github.io/chaguo-tanzania/ | grep -q "Chaguo"
          echo "✅ Website is up"
